    sudo chown -R www-data:www-data /var/www/wowonder
    sudo chmod -R 775 upload cache themes/*/cache config.php nodejs/config.json nodejs/models/wo_langs.js
    # Khởi chạy MySQL qua Docker (port 3434)
    mkdir -p db_data && docker compose -f docker-compose.db.yml up -d